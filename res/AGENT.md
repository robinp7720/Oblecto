# Resources (res/)

What lives here
- `config.json` is the default configuration template used by `oblecto init`.

Notes
- Update this file only when changing default config values or schema.
- Keep keys aligned with `src/interfaces/config.ts`.
