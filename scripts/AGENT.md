# Scripts (scripts/)

What lives here
- `test.sh` runs startup smoke tests and sets up `/etc/oblecto` if missing.

Notes
- Requires `dist/` to exist (run `npm run build` first).
- Creates `/etc/oblecto` and may need sudo on some systems.
